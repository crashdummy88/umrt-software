# Cloudflare Pages — umrt-verticals

1. Cloudflare Dashboard → Workers & Pages → Create → Connect to Git → `crashdummy88/umrt-verticals`
2. Production branch: `main`
3. Build command: *(empty)*
4. Build output directory: `/` (or leave default for static root)
5. Framework preset: **None**
6. Deploy → copy `*.pages.dev` URL into this README when known

No Functions required for v1 (catalog is static JSON). Soft-fail not needed until an API is added.
